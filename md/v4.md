你说得非常对！我之前的设计确实没有充分利用ERC1155的核心特性。让我重新设计一个更符合ERC1155标准的方案：

## ERC1155的核心特性

ERC1155的关键优势是：
- **同质化代币**：同一tokenId的NFT是可互换的
- **批量操作**：可以批量mint、transfer
- **节省gas**：比ERC721更高效

## 优化后的设计思路

### 核心概念
- **tokenId = eventId * 1000 + ticketType**
- 同一活动的同类型门票共享同一个tokenId
- 用户持有的是该tokenId的数量（balance），而不是唯一的NFT

### 1. **购票函数优化**

```solidity
// 利用ERC1155的批量mint特性
function buyTicketsByType(
  uint256 eventId,
  TicketType ticketType,
  uint256 count
) external payable {
  require(eventExists[eventId], "No event");
  EventStruct storage ev = events[eventId];
  require(count > 0, "Invalid count");
  require(block.timestamp < ev.startsAt, "Event already started");

  TicketTypeInfo storage ticketInfo = ev.ticketTypes[ticketType];
  require(ticketInfo.active, "Ticket type not active");
  require(msg.value >= ticketInfo.price * count, "Not enough ETH");
  require(ticketInfo.sold + count <= ticketInfo.capacity, "Ticket type sold out");

  // 计算tokenId
  uint256 tokenId = eventId * 1000 + uint256(ticketType);
  
  // 一次性mint多张相同类型的门票（利用ERC1155特性）
  ticketERC1155.mintTicket(msg.sender, tokenId, ticketInfo.metadataURI, count);

  // 记录购买信息（简化）
  TicketPurchaseRecord memory record;
  record.buyer = msg.sender;
  record.eventId = eventId;
  record.ticketType = ticketType;
  record.count = count;
  record.totalCost = ticketInfo.price * count;
  record.timestamp = block.timestamp;
  
  purchaseRecords[eventId].push(record);

  ticketInfo.sold += count;
  balance += msg.value;

  emit TicketPurchased(eventId, msg.sender, count);
}
```

### 2. **批量购买多种类型门票**

```solidity
// 利用ERC1155的批量mint特性 - 一次购买多种类型
function buyMultipleTicketTypes(
  uint256 eventId,
  TicketType[] memory ticketTypes,
  uint256[] memory counts
) external payable {
  require(ticketTypes.length == counts.length, "Length mismatch");
  require(eventExists[eventId], "No event");
  
  EventStruct storage ev = events[eventId];
  require(block.timestamp < ev.startsAt, "Event already started");

  uint256 totalCost = 0;
  uint256[] memory tokenIds = new uint256[](ticketTypes.length);
  string[] memory uris = new string[](ticketTypes.length);

  // 验证并计算总价
  for (uint256 i = 0; i < ticketTypes.length; i++) {
    TicketTypeInfo storage ticketInfo = ev.ticketTypes[ticketTypes[i]];
    require(ticketInfo.active, "Ticket type not active");
    require(ticketInfo.sold + counts[i] <= ticketInfo.capacity, "Sold out");
    
    totalCost += ticketInfo.price * counts[i];
    tokenIds[i] = eventId * 1000 + uint256(ticketTypes[i]);
    uris[i] = ticketInfo.metadataURI;
    
    ticketInfo.sold += counts[i];
  }

  require(msg.value >= totalCost, "Not enough ETH");

  // 批量mint - 这是ERC1155的核心优势！
  ticketERC1155.mintBatch(msg.sender, tokenIds, counts, uris);

  balance += msg.value;
  emit BatchTicketPurchased(eventId, msg.sender, ticketTypes, counts);
}
```

### 3. **检票系统 - 消费而非标记**

```solidity
// 检票时burn掉NFT（真正消费门票）
function checkInWithBurn(
  uint256 eventId,
  address attendee,
  TicketType ticketType
) external {
  require(eventCheckers[eventId][msg.sender], "Not authorized checker");
  require(eventExists[eventId], "Event not exists");

  uint256 tokenId = eventId * 1000 + uint256(ticketType);
  uint256 nftBalance = ticketERC1155.balanceOf(attendee, tokenId);
  require(nftBalance > 0, "No valid ticket");

  // Burn掉一张门票（真正消费）
  ticketERC1155.burn(attendee, tokenId, 1);

  // 记录检票
  checkInRecords[eventId][attendee] = CheckInRecord({
    attendee: attendee,
    ticketType: ticketType,
    timestamp: block.timestamp,
    checker: msg.sender
  });

  emit TicketChecked(eventId, msg.sender, attendee, ticketType);
}

// 或者保留门票但标记已使用
function checkInWithMark(
  uint256 eventId,
  address attendee,
  TicketType ticketType
) external {
  require(eventCheckers[eventId][msg.sender], "Not authorized checker");
  require(eventExists[eventId], "Event not exists");

  uint256 tokenId = eventId * 1000 + uint256(ticketType);
  uint256 nftBalance = ticketERC1155.balanceOf(attendee, tokenId);
  require(nftBalance > 0, "No valid ticket");

  // 检查是否已经检票
  bytes32 checkInKey = keccak256(abi.encodePacked(eventId, attendee, ticketType));
  require(!usedTickets[checkInKey], "Already checked in");
  
  usedTickets[checkInKey] = true;

  emit TicketChecked(eventId, msg.sender, attendee, ticketType);
}
```

### 4. **批量检票**

```solidity
// 利用ERC1155批量操作 - 批量检票
function batchCheckIn(
  uint256 eventId,
  address[] memory attendees,
  TicketType[] memory ticketTypes
) external {
  require(eventCheckers[eventId][msg.sender], "Not authorized");
  require(attendees.length == ticketTypes.length, "Length mismatch");

  for (uint256 i = 0; i < attendees.length; i++) {
    checkInWithMark(eventId, attendees[i], ticketTypes[i]);
  }
}
```

### 5. **门票转让（利用ERC1155的可转让性）**

```solidity
// 用户可以直接使用ERC1155的safeTransferFrom转让门票
// 在TicketERC1155合约中添加限制：

function _beforeTokenTransfer(
  address operator,
  address from,
  address to,
  uint256[] memory ids,
  uint256[] memory amounts,
  bytes memory data
) internal override {
  // 从tokenId解析eventId
  for (uint256 i = 0; i < ids.length; i++) {
    uint256 eventId = ids[i] / 1000;
    
    // 检查活动是否允许转让
    if (from != address(0) && to != address(0)) { // 不是mint或burn
      EventStruct storage ev = events[eventId];
      require(block.timestamp < ev.startsAt, "Event started, no transfer");
      
      // 检查门票是否已被使用
      TicketType ticketType = TicketType(ids[i] % 1000);
      bytes32 checkInKey = keccak256(abi.encodePacked(eventId, from, ticketType));
      require(!usedTickets[checkInKey], "Ticket already used");
    }
  }
  
  super._beforeTokenTransfer(operator, from, to, ids, amounts, data);
}
```

## ERC1155优势对比

| 特性 | ERC721方式 | ERC1155方式 |
|------|-----------|-------------|
| Gas成本 | 高（每张票单独mint） | 低（批量mint） |
| 存储成本 | 高（每张票独立存储） | 低（同类型共享tokenId） |
| 转让灵活性 | 单张转让 | 批量转让 |
| 同质化 | 每张票唯一 | 同类型票可互换 |
| 市场流通 | 需要每张票单独上架 | 可以批量交易 |

## 实际使用场景

```solidity
// 用户购买：2张VIP票 + 3张普通票
TicketType[] memory types = [TicketType.VIP, TicketType.GENERAL];
uint256[] memory counts = [2, 3];
buyMultipleTicketTypes(eventId, types, counts);

// 用户转让：转1张VIP票给朋友
ticketERC1155.safeTransferFrom(msg.sender, friend, vipTokenId, 1, "");

// 检票时：检查用户VIP票余额
uint256 vipBalance = ticketERC1155.balanceOf(user, vipTokenId);
if (vipBalance > 0) {
  // 允许进入VIP区
  checkInWithMark(eventId, user, TicketType.VIP);
}
```

这样的设计才真正发挥了ERC1155的优势：**同质化、批量操作、低gas成本**，同时保持了NFT的特性和市场流通性！